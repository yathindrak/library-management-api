# This is the main configuration file for the application.
# https://www.playframework.com/documentation/latest/ConfigFile
application.secret="YathindraKodithuwakku"

# Allowed hosts (should be changed in production)
play.filters.hosts {
  allowed = ["."]
}